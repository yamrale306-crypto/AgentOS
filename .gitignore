node_modules/
.next/
out/
dist/
.turbo/
*.tsbuildinfo
.env
.env.local
.env.development
.env.production
.env.*.local
!.env.example
*.log
.DS_Store

# Update staging area — never commit incoming code, backups, or extractions
new-update/backups/
new-update/extracted-code/
new-update/extracted/
# Transient plan files regenerate on every `plan`/`run`
new-update/UPDATE_PLAN.json
new-update/UPDATE_PLAN.md
# Python bytecode from the update tool
__pycache__/
*.pyc

# ---------- SECRETS — never commit ----------
# Local development credential files (may contain real API keys).
Dev Keys.txt
Dev\ Keys.txt

# Legacy nested repository (contains its own .git and local credential files).
# It was historically linked into the monorepo index as a gitlink with no
# .gitmodules mapping. It is not part of the pnpm workspace; keep it ignored
# so its contents can never be accidentally tracked by this repo.
AgentOS/
!AgentOS/README.md
# ---------- SECRETS — never commit ----------
# Local development credential file. NEVER commit or move into the repo.
Dev Keys.txt
dev-keys*.txt
*.secret
*.secret.*
*.key
*.pem
*.jwk
*.jwt
.env.production.local
credentials.json
id_rsa
id_ed25519
# Uncomment to also ignore everything handed in for review (keep README/MANIFEST):
# new-update/*
# !new-update/README.md
# !new-update/UPDATE_MANIFEST.md
# !new-update/update_tool.py
credentials.json
